# Jackson Annotation Processing Experiment

A little project to play around with different jackson annotations.

To see the different experiments, checkout the repository and then run:

    ./mwnw test
